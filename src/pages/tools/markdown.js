import React from 'react';
import ToolPage from '../../components/ToolPage';

export default function MarkdownEditorPage() {
  return (
    <ToolPage
      title="Markdown Editor"
      description="Live preview markdown editor with real-time rendering"
      comingSoon={true}
    />
  );
}