import React from 'react';
import ToolPage from '../../components/ToolPage';

export default function WHOISLookupPage() {
  return (
    <ToolPage
      title="WHOIS Lookup"
      description="Domain information and WHOIS lookup tool"
      comingSoon={true}
    />
  );
}